@model List<CategoryVM>
@{
}


<div class="container">
    <div class="container-fluid py-2 bg-light">
        <h6 class="display-6">
            <p class="text-capitalize fs-3">Categories Page</p>
        </h6>
    </div>
    <div class="row">
        <div class="col-md-4 offset-md-2">
            <a class="btn btn-primary" asp-controller="Categories" asp-action="Save">New Category</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <table class="table table-striped">
                <thead>
                <th>#</th>
                <th>Name</th>
                <th>Actions</th>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Name</td>
                            <td>
                                <a class="btn btn-warning" asp-controller="Categories" asp-action="Update" asp-route-id="@item.Id">Update</a>
                                <a class="btn btn-danger" asp-action="Remove" asp-controller="Categories" asp-route-id="@item.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>
</div>


